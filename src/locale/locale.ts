export default interface Locale {
  search: {
    destination: string;
  };
  emptySearch: {
    message: string;
    description: string;
  };
  cities: {
    paris: string;
    london: string;
  };
  countries: {
    france: string;
    uk: string;
  };
}
